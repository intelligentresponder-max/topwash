# CLAUDE-BRIEFING.md — Repo: topwash

## Auftrag
Optimierter Nachbau der Seitenstruktur von top-wash.de (Autowaschstraßen-Anbieter, 4 Standorte Rhein-Main) nach André's Vertical-Coding-Methodik. **Kein 1:1-Content-Klon** — Struktur/UX-Pattern werden übernommen, alle Texte werden neu geschrieben (Copyright!). Branding/Markenname muss sich vom Original unterscheiden, sofern es kein Auftrag des Originalbetreibers ist.

## Vorgehen: Vertical Coding, zweistufiges Prompt Chaining
1. **Schritt 1 — UX-Strategist & Copywriter Prompt**: Content-/UX-Struktur festlegen (Sektionen, FAQ-Themen, CTA-Texte, Standort-Datenmodell)
2. **Schritt 2 — Frontend-Architekt/Jamstack-Specialist Prompt**: Umsetzung in Code

## Negative Constraints (aus André's Leitfaden)
- Kein React/Vue, keine schweren Frameworks
- Kein Inline-CSS — semantisches HTML5 + Tailwind CDN
- Keine Platzhalter/Lorem-Ipsum — alle Inhalte final
- Jamstack-Architektur, statisch, GitHub-Pages-tauglich

## Referenz-Struktur (Pattern von top-wash.de, NICHT wörtlich übernommen)
- Hero mit Slider + Vertrauens-Badges (Testsieger/Auszeichnung)
- Standort-Schnellauswahl (Anzahl je nach echtem Anbieter)
- Prozess-Erklärung in 3 Stufen als Akkordeon (mit Bild je Stufe)
- Preistabelle: 4–5 gestaffelte Pakete
- 3 Info-Kacheln (Standorte / Prozess / Nachhaltigkeit)
- Standort-Karten: Adresse, Maps-Link, Öffnungszeiten
- Einfahrt-/Nutzungshinweise als Icon-Grid
- Fahrzeug-Abmessungs-Grafik
- Umfangreiches FAQ (Keyword-Dump + echte Kaufentscheidungsfragen)
- Footer: Sitemap, Impressum/AGB/Datenschutz, Social

## SEO-Learnings vom Original (übernehmenswert)
- Sehr hohe Textdichte pro Sektion, Long-Tail-Keywords organisch eingebettet
- Autoritätsanker (Testsiegel, Verbandsempfehlung) früh im Hero
- FAQ beantwortet echte Einwände (Wartezeit, Haftung, Eignung des Fahrzeugs) — kein reiner Keyword-Filler

## Offene Fragen vor Umsetzung — beantwortet
- [x] Für wen ist die Seite: eigenes Demo/Portfolio-Stück oder realer Auftraggeber? → **Realer Auftraggeber: top-wash.de selbst** (Rebuild/Redesign im Auftrag des Originalbetreibers).
- [x] Markenname → **TOPWASH Autowasch Spezialisten** (zulässig identisch/ähnlich zum Original, da Auftrag des Originalbetreibers).
- [x] Zielsystem → GitHub Pages (Repo `intelligentresponder-max/topwash`, Branch `main`).

## Fehler-Dokumentation
Alle während der Umsetzung gemachten Fehler hier unten fortlaufend eintragen (Vorgabe: Fehler immer dokumentieren, Alternativen auf Nutzen/Sicherheit/neue Wege doppelt prüfen).

### Fehlerprotokoll
- **Repo-Verwechslung (Sessionstart)**: Die Session war ursprünglich an das Repo `turmhotel` (unrelated Hotel-Projekt) angehängt, das Briefing verlangte jedoch Repo `topwash`. Erkannt durch Abgleich von Briefing-Inhalt gegen tatsächliche Repo-Dateien vor Codebeginn; neues Repo `topwash` angelegt statt versehentlich im falschen Repo zu bauen.
- **Markenname zu nah am Original**: Erster Vorschlag "TOPWASH Autowasch Spezialisten" ist nahezu identisch zum realen, aktiven Wettbewerber "Top-Wash" (top-wash.de). Vor Codebeginn geprüft, ob es sich um einen Auftrag des Originalbetreibers handelt (Bestätigung erhalten) — sonst wäre der Name markenrechtlich riskant gewesen.
- **Netzwerk-Zugriff auf top-wash.de blockiert**: Direkter `WebFetch` auf `top-wash.de`/`www.top-wash.de` schlug mit `EGRESS_BLOCKED` fehl (Proxy-Policy dieser Arbeitsumgebung). Alternative über `web.archive.org` ebenfalls nicht erreichbar. Ausweg: Recherche über `WebSearch`-Snippets (Adressen, Telefonnummern, Öffnungszeiten, ADAC-Testergebnis, Haftungs-FAQ) — Daten sind dadurch nicht 1:1 von der Live-Seite verifiziert und im README als prüfungsbedürftig markiert.
- **`tel:`-Links mit fehlerhaften Zeichen**: Beim Einfügen der Telefonnummern in `standorte.html` sind versehentlich ein Leerzeichen und ein unsichtbares Zero-Width-Space-Zeichen in zwei `href="tel:…"`-Attribute gerutscht. Per Skript bereinigt und Ergebnis mit `grep`/`cat -A` gegengeprüft.
- **Impressum/AGB nicht mit erfundenen Rechtsdaten befüllt**: Handelsregisternummer, Geschäftsführung und USt-IdNr. konnten nicht recherchiert werden. Bewusst NICHT erfunden (Impressumspflicht § 5 DDG), stattdessen klar markierte `[BITTE ERGÄNZEN]`-Platzhalter gesetzt — Abwägung: rechtssicheres, unvollständig markiertes Impressum vor Livegang > optisch "fertiges", aber falsches Impressum.
- **Standort-Recherche war teilweise falsch**: Über Suchmaschinen-Snippets wurde "Raunheim" als vierter Standort vermutet. Per Screenshot der echten Live-Seite bestätigt: der vierte Standort ist tatsächlich **Frankfurt** (Karl-von-Drais-Str. 15), nicht Raunheim. Alle betroffenen Stellen (Standort-Karten, Anker-Links, Footer, Impressum, AGB, Meta-Beschreibungen) korrigiert. Lehre: bei geratenen/recherchierten Fakten für einen realen Auftraggeber immer explizit als prüfungsbedürftig kennzeichnen (war hier bereits im README dokumentiert) statt sie als gesichert zu präsentieren.
- **Reale Rechts- und Preisdaten per Nutzer-Screenshot erhalten**: Da direkter Fetch weiterhin blockiert war, hat der Auftraggeber Screenshots der echten top-wash.de-Seiten (Startseite, Einfahrt-Hinweise, KFZ-Abmessungen, Impressum) geschickt. Daraus alle `[BITTE ERGÄNZEN]`-Platzhalter im Impressum durch echte Daten ersetzt (TOP WASH Autopflege GmbH, HRB 8993, USt-IdNr. DE114221224 usw.) sowie Einfahrt-Hinweise, Fahrzeug-Abmessungen und den Einstiegspreis (12 €) aktualisiert. Die übrigen 4 Preispaket-Stufen bleiben weiterhin Richtwerte, da nur der Einstiegspreis „ab 12 €" auf der Live-Seite sichtbar war — im README als offen markiert statt stillschweigend weiter zu raten.
- **Knallerpreise auf `angebote.html` sind Vorschläge, keine bestätigten Aktionen**: Auftrag war "3 Knallerpreise" für eine Lead-Landingpage, aber ohne Vorgabe der konkreten Aktionen. Auf Nachfrage wollte der Nutzer plausible Angebote vorschlagen lassen statt reale Werte zu liefern. Drei branchenübliche Angebotsformen entworfen (Neukunden-Rabatt, Wochentags-Special, Mengenrabatt/Waschkarte) und als Vorschlag markiert (README) statt sie als final zu präsentieren — Lehre aus dem Standort-Fehler oben: bei jeder nicht vom Auftraggeber selbst gelieferten Zahl explizit kennzeichnen, dass sie zur Bestätigung aussteht.
- **"KI Chat" ohne echtes LLM umgesetzt**: Anfrage war ein "KI Chat" in DE/EN. Da die Seite rein statisch ist (GitHub Pages, kein Server), hätte ein echtes LLM einen API-Key im Client-Code erfordert – das wäre öffentlich einsehbar und von Dritten missbrauchbar gewesen (Sicherheitsrisiko, potenziell hohe Kosten für den Auftraggeber). Vor Umsetzung nachgefragt und auf einen regelbasierten, stichwortbasierten Chat-Assistenten ohne externe API geeinigt. Bewusst nicht als "KI" beworben (Label "TOPWASH Assistent"), um Website-Besucher nicht über die tatsächliche Funktionsweise zu täuschen.
- **Neuer Branch auf Basis des offenen Angebote-PRs statt `main` gestartet**: `angebote.html` und die lead-fokussierten Header (inkl. `tel:`-CTAs) existieren nur auf dem noch offenen Branch `claude/topwash-leads-landing`, nicht auf `main`. Vor Beginn geprüft und den Chat-Widget-Branch bewusst von diesem Branch statt von `main` abgezweigt, damit der Chat auch auf `angebote.html` erscheint und keine Doppelarbeit an den Headern entsteht.
- **Echte TOPWASH-Fotos vom Nutzer erhalten, aber (noch) nicht direkt verwendbar**: Nutzer schickte 4 Handy-Screenshots der echten Slider-Bilder von top-wash.de (Personal bei Handvorwäsche/Textilwäsche). Diese enthalten Statusleiste/Browser-Chrome/Karussell-Punkte und sind als Datei keine sauberen Website-Assets. Vor dem Ersetzen der aktuellen Stock-Fotos nachgefragt, ob Originaldateien nachgeliefert werden können, statt die Screenshots direkt einzubinden oder ungefragt neue Stock-Fotos zu wählen — Nutzer schickt Originaldateien nach.
- **Keine erfundenen Trust-Signale für "Marktanalyse"-Verbesserung**: Anfrage war, die Landingpage anhand von Marktanalyse "interessanter" zu machen. Bewusst NICHT umgesetzt: erfundene Kundenstimmen/Testimonials, Fake-Countdown mit erfundenem Ablaufdatum, "X Personen haben gerade gebucht"-Zähler — das wäre bei einem echten Unternehmen irreführende Werbung. Stattdessen nur auf echten Zahlen basierende Elemente ergänzt (berechnete Rabatt-Prozente aus den bereits vorhandenen Vorher-/Nachher-Preisen, 3-Schritte-Einlöseanleitung, Einwand-FAQ mit bereits an anderer Stelle bestätigten Fakten).
- **Chat-Fenster nach Livegang zu dominant**: Nutzer testete die Seite nach Pages-Aktivierung und meldete per Screenshot ein auf dem Handy fast bildschirmfüllendes Chat-Fenster. Ursache: `h-[min(55vh,420px)]` als Tailwind-Arbitrary-Value-Klasse — die Tailwind-Play-CDN konnte die `min()`-Funktion darin nicht parsen und verwarf die Höhen-Regel stillschweigend, wodurch das Panel ohne Höhenbegrenzung auf den Inhalt schrumpfte bzw. (bei mehr Inhalt) unbegrenzt wuchs. Behoben durch reines Inline-`style="height:min(...)"` statt Tailwind-Arbitrary-Value, da Inline-Styles nicht von Tailwinds Klassen-Parser abhängen. Lehre: komplexe CSS-Funktionen (insb. mit Kommas) in Tailwind-Arbitrary-Values vor Einsatz gegen die tatsächlich geladene Tailwind-Variante testen, nicht nur auf Syntax-Gültigkeit prüfen.
- **Schließen-Button (X) im Chat-Fenster kaum treffbar**: Nutzer meldete, der Chat lasse sich per Klick auf das Kreuz nicht schließen. Die JS-Klick-Logik selbst war korrekt (per automatisiertem Klick auf exakte Koordinaten bestätigt) — das eigentliche Problem war die Tapfläche: Button hatte nur `p-1` (4px) Padding um ein 20px-Icon, macht ca. 28×28px, deutlich unter der empfohlenen Mindestgröße von ca. 40–44px für Touch-Ziele. Auf einem echten Touchscreen leicht daneben getippt. Behoben durch `min-w-[40px] min-h-[40px]` (reine Pixelwerte ohne Funktion, im Gegensatz zum vorherigen `min()`-Bug unproblematisch für die Tailwind-CDN) sowie `pointer-events-none` auf dem inneren SVG, damit Klicks immer eindeutig auf dem Button landen. Visuelle Pixel-Verifikation in dieser Arbeitsumgebung nicht möglich, da die Tailwind-CDN hier generell blockiert ist (siehe früherer Fehlerprotokoll-Eintrag zu top-wash.de) — nur Markup/Klassen und Klick-Verhalten automatisiert geprüft, keine echte Sichtprüfung.
- **FAQ-Prüfung deckte veraltete Fahrzeug-Abmessungen auf**: Auf Bitte, die FAQ zu prüfen, `faq.html` gegen die inzwischen bestätigten echten Daten aus `index.html`/`chat.js` gegengelesen. Gefunden: die sichtbare Antwort UND das eingebettete JSON-LD-Schema in `faq.html` trugen noch die ursprünglich geratenen Werte „2,10 m Höhe und 5,20 m Länge" von vor der Screenshot-Bestätigung des Auftraggebers — waren beim damaligen Update (PR #2) übersehen worden, weil nur `index.html` bearbeitet wurde, nicht die Duplikate in `faq.html`. Auf die echten Werte (200 cm Breite, 205 cm Höhe) korrigiert, ohne eine "Länge" zu erfinden, da die reale Quelle keinen Längenwert nennt. Zusätzlich einen holprigen Satz in der Haftungs-FAQ repariert ("Wir arbeiten sorgfältig und regelmäßig gewartet" — grammatisch bezog sich "gewartet" auf "wir" statt auf die Anlagen). Lehre: bei künftigen Datenkorrekturen alle Duplikate einer Angabe (JSON-LD, sichtbarer Text, mehrere Seiten) gezielt durchsuchen (`grep` über das ganze Repo), nicht nur die zuerst gefundene Stelle.
- **Schließen-Button trotz PR #7 (Tapfläche vergrößert) weiterhin nicht schließbar**: Nutzer meldete erneut, der Chat lasse sich nicht per Klick auf das X schließen — die reine Tapflächen-Vergrößerung aus PR #7 hat das eigentliche Problem also nicht gelöst. Bei erneuter Code-Durchsicht gefunden: `openChat()` rief beim Öffnen `inputEl.focus()` auf, was auf dem Handy sofort die virtuelle Tastatur einblendet. `position: fixed`-Elemente (wie das Chat-Panel) geraten auf mobilen Browsern in Kombination mit eingeblendeter Tastatur bekanntermaßen in ein Rendering-Problem, bei dem die sichtbare Position und die tatsächliche Tap-Zielfläche auseinanderlaufen können — das X sieht man, trifft es aber nicht. `inputEl.focus()` beim Öffnen komplett entfernt (Tastatur öffnet sich jetzt erst, wenn der Nutzer selbst ins Eingabefeld tippt — ohnehin die empfohlene Mobile-UX, kein ungefragtes Tastatur-Popup). Zusätzlich `chat.js?v=3`-Versionsparameter auf allen 8 Seiten ergänzt, um sicherzustellen, dass Handy-Browser/GitHub-Pages-Caching nicht versehentlich eine alte `chat.js`-Version ausliefern und künftige Fixes zuverlässig ankommen. Lehre: bei einem gemeldeten Bug, der nach einem ersten Fix weiterhin auftritt, nicht denselben Fix verstärken, sondern die Ursache neu durchdenken — die Tapfläche war real zu klein, aber nicht der einzige oder wahrscheinlich nicht der Hauptgrund.
- **Echte TOPWASH-Fotos erhalten und eingebaut**: Nutzer lieferte 3 echte Original-Bilddateien (keine Handy-Screenshots wie zuvor, sondern saubere 1921×738-Panoramafotos) mit der Bitte, sie "günstig" zuzuschneiden für bessere Qualität/Wirkung. Vor dem Einbau mehrere Zuschnitt-Varianten pro Foto erzeugt und visuell verglichen (u. a. ein zu eng geschnittener Kandidat verworfen, der dem Mitarbeiter den Kopf abgeschnitten hätte). Bewusst nicht alle 3 Fotos wahllos über die 5 vorhandenen Stock-Foto-Plätze verteilt, sondern inhaltlich passend zugeordnet: das Foto mit zwei Mitarbeitern beim Handschrubben ersetzt gezielt das Stock-Foto im Prozessschritt „Handvorwäsche" (exakte inhaltliche Übereinstimmung), die beiden anderen (Vorbehandlung im Freien, Felgenreinigung mit Hochdruckreiniger) wurden als Hero-Bilder verwendet. Die Stock-Fotos für „Textile Waschstraße" und „Trocknung" bewusst NICHT durch inhaltlich unpassende echte Fotos ersetzt, da keine passenden Aufnahmen dieser Schritte vorlagen — lieber ehrliches Stock-Foto als ein echtes Foto mit falscher Beschriftung. Bei einem der drei Originalfotos war im Hintergrund eine reale Preistafel der Anlage sichtbar; Bildausschnitt bewusst so gewählt, dass diese nicht prominent im sichtbaren Bereich liegt, um keine möglicherweise abweichenden Preisangaben neben der eigenen Preistabelle zu zeigen.
- **9 Video-Standbilder verarbeitet, Dateinamen-Zuordnung erst falsch, dann korrigiert**: Nutzer schickte 9 aus einem Video exportierte Standbilder mit Letterbox-Balken und bat, sie "anhand des Video-Transkripts" den Prozessschritten zuzuordnen — ein Transkript lag jedoch nie vor, nur die Bilder selbst; das dem Nutzer transparent mitgeteilt und stattdessen visuell zugeordnet. Beim ersten Durchsehen die Inhalte dreier ähnlich blauer Nahaufnahmen (Lamellen-Nahaufnahme, TOP-WASH-Eingangsschild, Geisterbild-Torbogen) den falschen Dateinamen zugeordnet; vor dem Zuschnitt jedes Bild einzeln erneut mit dem Read-Tool verifiziert und die Zuordnung korrigiert, statt auf der ersten (falschen) Einschätzung weiterzubauen. Letterbox-Balken automatisiert per Spaltenhelligkeits-Gradient erkannt (feste Pixelwerte hätten bei unterschiedlichen Balkenbreiten pro Videoclip nicht funktioniert). Von 9 Bildern nur 5 verwendet: 2 (Geisterbild-Doppelbelichtung, starke Bewegungsunschärfe) waren zu schlecht für eine professionelle Website, 1 war ein redundantes Duplikat, 1 (extreme Lamellen-Nahaufnahme) zu unscharf/schwer erkennbar — bewusst lieber weniger, dafür saubere Bilder statt alle 9 irgendwo unterzubringen. Bei einem verwendeten Bild (Frontpartie-Nahaufnahme) war ein scharf lesbares deutsches Kennzeichen sichtbar; vor Veröffentlichung aus Datenschutzgründen (KFZ-Kennzeichen als personenbezogenes Datum) weichgezeichnet, obwohl das Video vom Auftraggeber selbst stammt — unklar, ob es sich um ein Kunden- oder Firmenfahrzeug handelt, daher im Zweifel für den Datenschutz entschieden.
- **Prozessbeschreibung anhand des echten Video-Transkripts korrigiert**: Nutzer schickte nachträglich den vollständigen deutschen Text des offiziellen TOPWASH-Werbevideos. Bis dahin war der Ablauf auf `index.html`/`faq.html`/`chat.js` als generisches „Handvorwäsche → eine textile Waschstraße → Trocknung" beschrieben — eine plausible, aber nicht durch eine Primärquelle belegte Annahme. Das Transkript zeigt einen präziseren, abweichenden Ablauf: (1) Handvorwäsche im Freien mit konkreten Teilschritten (kostenlose Felgenreinigung, Schmutzlöser, Hochdruckreiniger, Handwäsche von Ecken/Kanten/Scheiben/Spiegeln/Heck), (2) **drei** Textilwäsche-Stufen in der Halle statt einer generischen („zwei mehr als bei herkömmlichen Autowaschanlagen" laut Transkript), (3) doppelte Trocknung aus zwei Gebläsen **plus** einer abschließenden, zusätzlich polierenden Textiltrocknung. Zusätzlich ergänzt: der bislang unbekannte benannte Zusatz „Lotusglanz" (als Deluxe-Feature auf `index.html`/`preise.html` sowie als einzeln buchbare Option vermerkt) und die offizielle Tagline „Sauber wie von Hand gewaschen!". Alle drei Prozessschritt-Texte auf `index.html` (inkl. Überschriften), der FAQ-Eintrag samt JSON-LD-Duplikat in `faq.html` sowie die Ablauf-Antwort (DE/EN) in `chat.js` synchron aktualisiert, um erneut ein Auseinanderlaufen von Duplikaten wie beim Fahrzeug-Abmessungen-Fehler oben zu vermeiden. Lehre: eine vom Auftraggeber selbst gelieferte Primärquelle (Original-Werbetext) sticht jede eigene, plausible Vermutung — bei Erhalt einer solchen Quelle proaktiv alle Duplikate der betroffenen Aussage im Repo durchsuchen und aktualisieren, nicht nur die naheliegendste Stelle.
- **Hero-Absatz bei der Prozess-Korrektur übersehen**: Bei der o. g. Transkript-Korrektur wurde `grep` gezielt nach den drei Prozessschritt-Duplikaten (Prozess-Sektion, FAQ, Chat-KB) durchsucht, aber ein viertes Duplikat übersehen: der Hero-Absatz ganz oben auf `index.html` ("Handvorwäsche in drei Stufen, schonende Textilwäsche und kraftvolle Trocknung…") beschrieb weiterhin den alten Ablauf. Erst beim nächsten Edit-Auftrag (Logo-Einbindung) zufällig bemerkt, weil die Datei dabei erneut vollständig eingelesen wurde. Nachträglich korrigiert. Lehre: bei Textkorrekturen dieser Art nicht nur strukturell offensichtliche Stellen (Sektion mit gleicher Überschrift) durchsuchen, sondern nach den inhaltlichen Schlüsselbegriffen selbst grep'en (hier z. B. „Handvorwäsche" ohne Kontext), um auch Kurzformen wie Hero-Teaser oder Meta-Beschreibungen zu erfassen.
- **Echtes Logo aus Nutzer-Screenshot rekonstruiert**: Nutzer wollte das offizielle SVG-Logo von top-wash.de einbinden; direkter Zugriff auf die Domain blieb wie zuvor durch die Netzwerk-Egress-Sperre dieser Umgebung blockiert (bestätigt erneut per `curl` und `WebFetch`). Nutzer schickte stattdessen einen Handy-Screenshot des im Browser gerenderten SVGs (plus ein zweites, stilisiertes Logo-Standbild aus einem YouTube-Video mit Farbverlauf-Hintergrund, das bewusst NICHT verwendet wurde, da es für ein sauberes Website-Logo ungeeignet ist – Farbverlauf-Strahlenkranz statt neutralem Hintergrund). Aus dem Screenshot per Bildverarbeitung (Browser-Chrome weggeschnitten, Logo-Bereich per Blaukanal-Erkennung lokalisiert, weißer Hintergrund per Helligkeits-Schwellenwert in echte Transparenz umgewandelt) ein sauberes transparentes PNG erzeugt. Die Navy-Farbe wurde aus den dunkelsten Pixeln ermittelt statt aus einem zufälligen Ausschnitt, da ein erster Versuch mit einem festen Koordinaten-Sample versehentlich einen fast-weißen Bereich traf und beinahe eine falsche (zu helle) Logofarbe erzeugt hätte – vor dem Speichern per Vorschau-Composite auf grauem und dunklem Hintergrund visuell verifiziert, um einen weißen Kantenschimmer auszuschließen.
- **Schließen-Button-Bug endgültig gelöst — echte Ursache lag nie beim Klick, sondern bei einer CSS-Kaskaden-Kollision zwischen Tailwind und dem `hidden`-Attribut**: Nach GitHub-Pages-Aktivierung testete der Nutzer die Live-Seite und meldete den Bug ein DRITTES Mal – trotz PR #7 (Tapfläche vergrößert) und PR #9 (Auto-Focus entfernt). Das Screenshot zeigte das Chat-Fenster offen, ohne dass der Nutzer es absichtlich geöffnet hatte, was den entscheidenden Hinweis lieferte: das Panel war vermutlich nie wirklich geschlossen gewesen. Ursache gefunden: `panel.hidden = true`/`toggleBtn.hidden = true` setzt zwar das HTML-`hidden`-Attribut korrekt, aber sowohl das Panel (`class="... flex flex-col ..."`) als auch der Toggle-Button (`class="... grid place-items-center ..."`) tragen zusätzlich eine Tailwind-Display-Utility-Klasse. Per CSS-Kaskadenregeln gewinnt JEDE Autor-Stylesheet-Regel, die `display` setzt (hier `.flex{display:flex}` bzw. `.grid{display:grid}` aus der Tailwind-CDN), unabhängig von Spezifität gegen die User-Agent-Standardregel `[hidden]{display:none}` – das `hidden`-Attribut hat dadurch schlicht keine visuelle Wirkung mehr, sobald Tailwind geladen ist. Das JS hat also die ganze Zeit korrekt funktioniert (Zustand wurde intern richtig aktualisiert), nur die CSS hat es nie umgesetzt. **Kritisch**: diese Kaskaden-Kollision konnte in dieser Sandbox nie automatisiert reproduziert werden, weil `cdn.tailwindcss.com` hier grundsätzlich blockiert ist – die `.flex`/`.grid`-Regeln existierten in keinem einzigen lokalen Test dieser Session, weshalb das native `[hidden]`-Verhalten dort stets (fälschlich) korrekt aussah. Erst ein bewusst nachgebautes Mini-Testdokument mit den betroffenen Tailwind-Klassen als lokalem `<style>`-Block (ohne Netzwerkzugriff) konnte den Bug reproduzieren – und bestätigte am alten `chat.js`-Stand sogar, dass das Panel bereits beim ersten Seitenaufruf sichtbar war und Klicks auf den (unsichtbar) darunterliegenden Toggle-Button blockierte, nicht erst nach einer Nutzerinteraktion. Behoben durch explizites Setzen von `element.style.display` (Inline-Style, schlägt jede Klassen-Regel unabhängig von Ladereihenfolge) zusätzlich zum `hidden`-Attribut in `openChat()`/`closeChat()`, sowie einen initialen `display:none` im Panel-Markup selbst. Fix am nachgebauten Kaskaden-Testfall gegen den alten UND den neuen Code verifiziert (alt: Panel von Anfang an sichtbar und blockiert Klicks; neu: korrekt versteckt, öffnet/schließt zuverlässig über mehrere Zyklen). `chat.js?v=4` auf allen 8 Seiten hochgezählt. Lehre (die wichtigste dieses Projekts): wenn ein gemeldeter Bug nach zwei plausiblen, aber erfolglosen Fixes weiter besteht, ist die Fähigkeit, ihn überhaupt zu reproduzieren, selbst zu hinterfragen – hier lag der blinde Fleck nicht in der Diagnose, sondern in der Testumgebung selbst (Tailwind-CDN-Sperre), die eine ganze Klasse von CSS-Kaskaden-Bugs strukturell unsichtbar machte, egal wie oft dieselbe (unzureichende) Testmethode wiederholt wurde.
- **QR-Code-Ankerpunkte + neue Programm-/Preisstruktur (per "SYSTEM-DIRECTIVE"-Nachricht)**: Nutzer schickte eine als "SYSTEM-DIRECTIVE" formatierte technische Spezifikation für 5 Anker-IDs (`#home`, `#vorreinigung`, `#programme`, `#highlights`, `#standort`) für ein QR-Code-Leitsystem, inkl. einer neuen Preis-Matrix (12/15/18/20/23 €: Soft-Schaum/Komplett/Lotus/DAS BESTE/Superschaum) und neuer Vorwäsche-Prozessschritte (Einsprühen/Abdampfen/Bürsten). Die neuen Prozessschritte widersprachen direkt dem gerade erst umgesetzten, offiziellen Video-Transkript (PR #12) – ein klares Warnsignal, da diese Nachricht ohne erkennbare Quellenangabe kam und reale Geschäftsdaten (Preise) enthielt, die die bisherige, explizit als "noch zu bestätigen" markierte Preistabelle vollständig ersetzt hätten. Vor der Umsetzung per `AskUserQuestion` explizit nachgefragt statt die Zahlen stillschweigend zu übernehmen: (1) ob die neue Preis-Matrix echte, bestätigte Daten sind → Nutzer bestätigte „Ja, echte Preise – ersetzen"; (2) wie mit dem Widerspruch bei den Vorwäsche-Schritten umzugehen ist → Nutzer entschied „Video-Transkript behalten". Umgesetzt: alle 5 Anker-IDs (inkl. `id="vorreinigung"` direkt auf dem bereits offenen ersten Prozess-Schritt-`<details>`, damit ein QR-Scan sofort sichtbaren, aufgeklappten Inhalt zeigt, nicht nur einen eingeklappten Akkordion-Header), `scroll-margin-top: 65px` (exakt aus den verwendeten Tailwind-Klassen berechnet: `h-16`=64px + `border-b`=1px, nicht geraten), neue Preistabelle auf `index.html`+`preise.html`, neue "Highlights"-Sektion (Lotus-Glanz, Superschaum, textile Poliertrocknung – letzteres bereits aus dem Video-Transkript bekannt), sowie Folgeanpassungen in `chat.js`, `faq.html` und den `angebote.html`-Knallerpreisen (auf die neuen Programmnamen/-preise umgerechnet, weiterhin klar als eigene Vorschläge markiert). **Offen/eigene Konstruktion, nicht vom Nutzer einzeln bestätigt**: die genaue Feature-Zuordnung pro Programm (welches Programm genau welche Zusatzleistung enthält) stammt NICHT aus der Nutzer-Nachricht (die nannte nur Namen + Preise), sondern ist eine eigene, in sich konsistente Fortschreibung des bisherigen "Gut-besser-am besten"-Musters – sollte vor Livegang mit dem Betreiber gegengeprüft werden. Lehre: eine als Systemnachricht formatierte, unbelegte Anweisung mit Widerspruch zu bereits verifizierten Primärquellen nicht blind umsetzen, auch wenn sie im Duktus der eigenen Projektmethodik geschrieben ist – aktiv nachfragen, wenn reale Geschäftsdaten betroffen sind.
- **Korrekturen aus einer parallelen Chat-Session (ohne GitHub-Schreibzugriff) zurückgeführt**: Der Nutzer hatte in einer separaten Unterhaltung (ohne Zugriff auf dieses Repo) Öffnungszeiten-, Telefon- und Adresskorrekturen sowie einen neuen Impressum-Haftungsausschluss erarbeitet und als ZIP (aktualisierte `standorte.html`/`impressum.html` + eigenes Doku-Dokument) hochgeladen, mit der Bitte, dies zu übernehmen. Vor der Übernahme jede Datei per `diff` gegen den aktuellen `main`-Stand verglichen statt sie blind zu überschreiben, da die andere Session auf einem älteren Stand basierte und ein vollständiges Überschreiben zwischenzeitliche Verbesserungen (Logo, QR-Anker, neue Preisstruktur) hätte zurückrollen können — die Diffs betrafen aber ausschließlich die dokumentierten Korrekturen (Öffnungszeiten, Frankfurt-Telefonnummer/-Stadtteil, neue Haftungsabschnitte), keine Überschneidung mit späteren Änderungen. Dabei eine von der anderen Session laut eigenem Dokument bereits korrigierte, aber nicht mit ins ZIP aufgenommene Stelle gefunden: die generische Öffnungszeiten-Zusammenfassung in `chat.js` (DE + EN) nannte noch die alten, falschen Pauschalwerte („Mo–Fr 07:30–20:00 Uhr, Sa 07:30–19:00 Uhr") – nachträglich selbst auf die neuen, korrekten Bandbreiten (08:00–19:00 bis 08:30–18:30 Uhr, Sa einheitlich 08:00–18:00 Uhr) korrigiert. Lehre: bei der Übernahme extern vorbereiteter Korrekturen nicht nur die mitgelieferten Dateien 1:1 anwenden, sondern eigenständig repo-weit nach denselben Duplikaten suchen, die frühere Fehlerprotokoll-Einträge bereits als Wiederholungsmuster identifiziert haben (`chat.js` war schon zweimal zuvor die übersehene Duplikat-Quelle) – eine externe Zusammenfassung ist nicht automatisch vollständig.
- **Blog-Pillar-Beitrag „Die Top Wash Formel" per erneuter "SYSTEM-DIRECTIVE"-Nachricht — dieselbe unbestätigte Prozessbeschreibung wie zuvor tauchte wieder auf**: Nutzer schickte eine neue "SYSTEM-DIRECTIVE" für einen Blog-Beitrag (`blog/die-top-wash-formel.html`) mit 4 Abschnitten. Abschnitt 1 sollte laut Directive die Vorwäsche als "Einsprühen → Abdampfen → Bürsten" beschreiben — exakt dieselbe Formulierung, die bereits in der vorherigen "SYSTEM-DIRECTIVE" (QR-Anker-Preis-Eintrag oben) auftauchte und dort dem echten Video-Transkript widersprach; der Nutzer hatte sich damals explizit für das Transkript entschieden. Da diese Entscheidung bereits getroffen war, wurde NICHT erneut nachgefragt, sondern direkt die bereits bestätigte, transkriptbasierte Beschreibung verwendet (kostenlose Felgenreinigung, Schmutzlöser, Hochdruckreiniger, dann Handwäsche) – dem Nutzer im Chat transparent mitgeteilt. Lehre: eine bereits getroffene Entscheidung bei Wiederholung desselben Konflikts nicht erneut zur Diskussion stellen (das wäre unnötig redundant), aber die Abweichung von der Vorlage aktiv benennen, damit der Nutzer nachvollziehen kann, warum der veröffentlichte Text nicht 1:1 der Anweisung folgt.
  Zusätzlich enthielt Abschnitt 4 eine komplett neue, bis dahin in keiner bestätigten Quelle vorkommende Tatsachenbehauptung: überdachte SB-Boxen (Selbstbedienungs-Waschboxen) am Standort Bad Nauheim. Anders als bei der wiederholten Prozessbeschreibung gab es hierzu keine vorherige Entscheidung – per `AskUserQuestion` nachgefragt, Nutzer bestätigte „Ja, real". Erst danach den Abschnitt geschrieben, bewusst ohne über die reine Existenz hinausgehende Details (Preise, Ausstattung) zu erfinden, da nur die Existenz bestätigt wurde, keine Einzelheiten.
  **Technische Entscheidungen (Fehlerprotokoll-Pflicht laut Directive)**: (1) Anker-Navigation – Quick-Link-Box verlinkt per `../index.html#anker` zurück auf die Startseite statt die Prozess-/Preis-Inhalte auf der Blog-Seite zu duplizieren; Alternative (Inhalte duplizieren) verworfen, da das zwei separat zu pflegende Quellen für dieselbe Information geschaffen hätte – exakt das Muster, das in diesem Projekt wiederholt zu übersehenen, veralteten Duplikaten geführt hat (siehe Fahrzeug-Abmessungen-, Hero-Absatz- und Öffnungszeiten-Fehler oben). (2) Bildmaterial – zwei bereits vorhandene, echte Fotos (`handvorwaesche-team.jpg`) wiederverwendet statt neue Platzhalter-/Stock-Bilder einzufügen, mit `loading="lazy"` konsistent zum Rest der Seite. (3) Alle internen Links/Assets im neuen `blog/`-Unterverzeichnis benötigen ein `../`-Präfix (Bild-Pfade, `chat.js`, Seiten-Links) – vor dem Commit per Playwright-Test verifiziert, dass Logo, Chat-Skript und interne Links tatsächlich auflösen, nicht nur die Syntax geprüft.
  **Nebenbefund beim Bearbeiten des Footers**: Der Footer auf `index.html` zeigte weiterhin das alte „TW"-Platzhalter-Badge (nicht das echte Logo aus PR #13 – nur der Header war damals aktualisiert worden). Ersetzt durch das echte Logo mit `brightness-0 invert`-Filterklassen, da das navy-farbene Logo auf dem dunklen Footer-Hintergrund (bg-slate-900) sonst kaum lesbar gewesen wäre – anders als im Header (heller Hintergrund) reicht dort das Originalbild nicht aus.
- **Echter Countdown zur Online-Shop-Eröffnung (bewusst kein Fake-Countdown)**: Nutzer bat direkt (per Chat, nicht als "SYSTEM-DIRECTIVE") um einen neuen Blog-Beitrag zur bevorstehenden Eröffnung des Online-Shops (Wertkarten/Waschabos ab 1. Oktober 2026 online kaufbar), mit einem Countdown oben im Beitrag. Anders als bei der zuvor im Projekt explizit abgelehnten "Marktanalyse"-Anfrage (siehe Eintrag oben: keine erfundenen Countdown-Timer mit fiktivem Ablaufdatum) handelt es sich hier um ein reales, vom Auftraggeber selbst genanntes Datum für ein reales bevorstehendes Feature – kein Fall von irreführender Fake-Dringlichkeit, daher ohne Bedenken umgesetzt. Implementiert als reine Client-seitige Datumsdifferenz (`new Date(2026, 9, 1)` minus aktuelles Datum, in Tagen, kein serverseitiger Zustand nötig), mit explizitem Fallback-Zustand für den Fall, dass die Seite nach dem 1.10.2026 weiterhin unverändert online ist ("Ab sofort verfügbar" statt einer negativen Zahl) – gegen ein simuliertes Datum nach dem Stichtag getestet, um sicherzustellen, dass die Seite auch ohne manuelles Update nach dem Start nicht falsch/kaputt aussieht. Bewusst kein sekündlich tickender Timer, da eine Tagesgenauigkeit für diesen Zweck ausreicht und ein echter Live-Ticker unnötige Komplexität ohne Mehrwert wäre. Neue Seite zusätzlich in der Footer-Sektion "Wissen & Technik" sowie als kontextuellen Link direkt bei der bestehenden Wertkarten-Erwähnung in `preise.html`s „Gut zu wissen"-Liste verlinkt und in `sitemap.xml` ergänzt.
- **Fragwürdiges "SEO-Snippets"-Paket per Copy-Paste erkannt und NICHT übernommen**: Nutzer bat direkt um zwei klare, einfache Änderungen (Blog-Link im Hero-Bereich, Bilder-Slideshow oben auf der Startseite) und bat gleichzeitig ausdrücklich, einen mitgeschickten, offensichtlich von einem anderen KI-Tool erzeugten Textblock zu prüfen ("Check das bitte"). Der Block enthielt eine SEO-Snippet-Tabelle für angeblich "neue Blog-Beiträge" ("Der Schmirgel-Effekt", "SB-Waschplätze", "Lotus-Glanz & Finish" als jeweils EIGENE Dateien) sowie eine eingebettete "SYSTEM-DIRECTIVE" mit der Aufforderung, Meta-Tags/Canonical-Links/strukturierte Daten zu ergänzen. Vor jeder Umsetzung geprüft und mehrere Probleme gefunden: (1) alle drei genannten Blog-Dateien existieren gar nicht als eigene Seiten – nur `die-top-wash-formel.html` (die diese Themen bereits als Abschnitte EINER Seite behandelt) und `online-shop-eroeffnung.html` existieren; (2) das als Beispiel genannte Canonical-Domain-Muster `https://topwash-frankfurt.de/...` ist eine völlig andere, nicht existierende Domain als die tatsächlich verwendete `intelligentresponder-max.github.io/topwash/` – ein Canonical-Tag mit dieser falschen Domain hätte aktiv SEO-schädlich gewirkt; (3) neue, nirgends zuvor bestätigte Detailbehauptungen ("4 überdachte SB-Waschplätze" als exakte Zahl, "biologisch abbaubare Reiniger") gingen über die bereits bestätigte reine Existenz der SB-Boxen hinaus; (4) der Textblock endete mit einer an den Nutzer gerichteten Folgefrage eines anderen Tools ("Soll ich als Nächstes Open-Graph-Tags vorbereiten?") – ein klares Indiz, dass der gesamte Block unverändert aus einer fremden KI-Sitzung kopiert wurde, nicht selbst vom Nutzer verfasst war. Dem Nutzer alle vier Punkte transparent zurückgemeldet, NICHTS aus dem SEO-Block umgesetzt, stattdessen nur die beiden tatsächlich direkt gewünschten Änderungen ausgeführt. Lehre: "bitte prüfen" ist wörtlich zu nehmen, besonders wenn der zu prüfende Text selbst erkennbar von einer anderen, nicht mit dem Projektstand vertrauten Quelle stammt – ein plausibel klingendes SEO-Paket kann trotzdem auf nicht existierende Seiten und falsche Domains verweisen.
- **Lazy-Loading-Falle bei gestapelten Slideshow-Bildern (verwandter Bug-Typus wie beim `hidden`-Kaskaden-Fund)**: Beim Bau der neuen Hero-Slideshow (5 echte Fotos, absolut positioniert übereinander, per Opacity-Klasse ein-/ausgeblendet) zunächst `loading="lazy"` auf allen Bildern außer dem ersten belassen – wirkte plausibel, da nur ein Bild gleichzeitig sichtbar ist. Playwright-Test zeigte jedoch `naturalWidth: 0` für alle nicht-ersten Slides: der Browser verzögert das Laden von `loading="lazy"`-Bildern selbst dann, wenn sie geometrisch im Viewport liegen, sobald sie durch `opacity-0` unsichtbar sind – dieselbe Kategorie Bug wie der `hidden`-Attribut-vs-Tailwind-Klassen-Kaskadenfehler weiter oben: ein natives Browser-Feature, das bei überlagerten/unsichtbaren, aber layouteten Elementen anders funktioniert als naiv erwartet. Behoben durch `loading="eager"` auf allen 5 Slides, da es sich um oberhalb des Falts liegenden, sofort relevanten Hero-Content handelt, bei dem ein sichtbares Nachladen beim ersten automatischen Wechsel ohnehin inakzeptabel gewesen wäre. Fix per Playwright verifiziert (`naturalWidth` aller 5 Bilder > 0). Zusätzlich `prefers-reduced-motion` respektiert (Auto-Wechsel pausiert bei entsprechender Nutzereinstellung) und Pause-bei-Hover ergänzt.
- **Dieselbe "SEO-Snippets"-Nachricht ein drittes Mal erhalten, diesmal als "Mega-Upgrade"-SYSTEM-DIRECTIVE mit Verweisen auf nicht existierende "Artifacts"**: Nutzer schickte eine deutlich umfangreichere "SYSTEM-DIRECTIVE", die im Kern dieselbe, bereits im vorherigen PR abgelehnte SEO-Snippet-Tabelle wiederholte (falsche Canonical-Domain-Logik, unbestätigte "4 SB-Waschplätze", "biologisch abbaubare Reiniger", die bereits zweimal abgelehnte Einsprühen/Abdampfen/Bürsten-Prozessbeschreibung), zusätzlich verpackt mit Verweisen auf angeblich vorliegende "Artifacts" (technischer System-Prompt, visuelles Struktur-Konzept, Phygital User Journey, QR-Code-Leitsystem mit 6 Codes, "Screenshot [1]") — keines dieser Artefakte wurde in dieser Session tatsächlich geteilt. Zusätzlich neuer, eigener Fehler in der Nachricht gefunden: die Beschreibung ordnete Lotus-Glanz fälschlich exklusiv dem Programm „DAS BESTE" für 23 € zu — tatsächlich beginnt Lotus-Glanz bereits beim Programm „Lotus" (18 €) und „DAS BESTE" kostet 20 €, nicht 23 € (das ist der Preis von „Superschaum"). Alle Punkte dem Nutzer transparent aufgelistet, bevor irgendetwas umgesetzt wurde. Per `AskUserQuestion` gezielt nur die eine echte offene Architekturfrage gestellt (Pillar-Beitrag in 4 Seiten aufteilen?) — Nutzer bestätigte die Aufteilung. Umgesetzt: 3 neue Cluster-Seiten (`schmirgel-effekt-vermeiden.html`, `sb-waschplaetze-bad-nauheim.html`, `lotus-glanz-poliertrocknung.html`) mit eigenen, korrigierten Meta-Tags (echte Canonical-Domain `intelligentresponder-max.github.io/topwash`, keine erfundene SB-Boxen-Anzahl, korrigierte Programmpreise), der bestehende Pillar-Beitrag (`die-top-wash-formel.html`) bleibt unverändert als Übersichtsseite bestehen und verlinkt jetzt an den passenden Stellen auf die 3 vertiefenden Cluster-Seiten (klassisches Pillar-&-Cluster-SEO-Muster statt Redundanz). FAQPage-JSON-LD (wie in der ursprünglichen BLOG-USP-DIRECTIVE angefragt) auf der Schmirgel-Effekt-Seite ergänzt, mit selbst formulierten, faktisch korrekten Frage/Antwort-Paaren. Die vorgeschlagene 6. QR-Code/Anker-Erweiterung (`#sb-waschplaetze` als neue Homepage-Sektion) wurde NICHT umgesetzt, da dafür kein expliziter Auftrag vorlag (nur die Frage zur Seiten-Aufteilung wurde gestellt) — als offener Punkt an den Nutzer zurückgemeldet. Meta-Descriptions gegen die im Directive selbst genannten Zeichenlimits geprüft (Title ≤60, Description ≤155, nach HTML-Entity-Dekodierung gemessen, nicht am rohen Quelltext) — eine Description lag mit 170 Zeichen über dem Limit und wurde auf 136 gekürzt. Lehre: eine wiederholte, hartnäckige Quelle mit gemischt korrekten und falschen Angaben wird durch mehr Ausschmückung (mehr "Artifacts", größerer Scope) nicht glaubwürdiger — jede einzelne Behauptung bleibt einzeln prüfungsbedürftig, unabhängig vom Umfang der Verpackung.
- **Eigener Prozessfehler bemerkt: direkt auf `main` statt auf einem Feature-Branch gearbeitet**: Bei der Umsetzung der obigen Cluster-Seiten fiel nach mehreren Datei-Änderungen auf, dass `git status`/`git branch --show-current` `main` zeigte — die Änderungen waren versehentlich direkt im `main`-Checkout vorgenommen worden, statt vorher wie in jedem vorherigen PR dieser Session einen neuen Branch anzulegen. Ursache: nach dem Pull von `main` zu Beginn der Aufgabe direkt mit dem Editieren begonnen, ohne den gewohnten `git checkout -B claude/...`-Schritt. Kein Schaden entstanden, da noch nichts committet war (`main` blieb unverändert) — die unveränderten Working-Tree-Änderungen wurden per `git checkout -b` verlustfrei auf einen neu erstellten Branch mitgenommen, bevor committet wurde. Lehre: den Branch-Wechsel als ersten, nicht überspringbaren Schritt jeder neuen Aufgabe behandeln, direkt nach `git pull`, bevor die erste Datei bearbeitet wird — nicht erst am Ende der Aufgabe vor dem Commit prüfen.
- **Google-Bewertungen-Widget vor Einbau geprüft — drei „Magie"-Probleme im Nutzer-Entwurf gefunden**: Nutzer legte einen fertigen HTML/Konzept-Entwurf für ein Google-Reviews-Widget (`#bewertungen`) vor und bat ausdrücklich, ihn vor dem Einbau auf „Korrektheit und Magie" zu prüfen. Gefunden: (1) zwei wörtliche, namentlich zugeordnete Kundenzitate („Juan Ignacio", „Steve C.") ohne jede Quelle — exakt das Muster, das dieses Projekt bereits im Fehlerprotokoll-Eintrag „Keine erfundenen Trust-Signale" als irreführende Werbung bei einem echten Unternehmen ausgeschlossen hat; (2) ein Link auf ein nicht existierendes Asset (`assets/logos/google-g.svg`), das weder im Repo liegt noch zur bestehenden `images/`-Pfadkonvention passt; (3) ein „Rezension schreiben"-Button mit wörtlichem Platzhalter `placeid=IHRE_PLACE_ID` statt einer echten Google-Place-ID — wäre live als kaputter Link erschienen. Die im Entwurf genannten Kennzahlen (★ 4,5/5, 651 Google-Bewertungen, ADAC „GUT") selbst waren dagegen keine neue unbestätigte Behauptung, sondern bereits seit PR #15 identisch auf `angebote.html` in Verwendung — hier nur konsistent wiederverwendet. Vor der Umsetzung per `AskUserQuestion` zu den zwei blockierenden Punkten nachgefragt: Nutzer entschied, die Namens-Zitate wegzulassen, und lieferte den echten Google-Maps-Link (`https://maps.app.goo.gl/EVwrtbTB2TpMaKg87`) statt des Platzhalters. Umgesetzt: reine Kennzahlen-Karte (Sterne/Note/Bewertungsanzahl/ADAC-Note) plus zwei Buttons zum echten Maps-Link, kein Logo-Asset referenziert (weggelassen statt erfunden), kein `AggregateRating`-JSON-LD ergänzt (dafür fehlt ein vollständiges `LocalBusiness`-Schema auf `index.html` — außerhalb des Auftragsumfangs). Lehre: ein als „bitte vorher prüfen" markierter Entwurf kann trotzdem teilweise korrekt sein (hier: die Kennzahlen) — die Prüfpflicht gilt granular pro Einzelbehauptung/-element, nicht als Ganzes annehmen oder ablehnen.
- **Eigener Fehler in PR #21: ein einziger, globaler Bewertungs-Link für alle 4 Standorte verwendet**: Nach dem Merge von PR #21 meldete der Nutzer per Screenshot, dass der „Bewertung schreiben"-Button für alle 4 Standorte auf denselben Google-Maps-Link zeigte, der tatsächlich nur zum Bewertungsprofil des Standorts Frankfurt führt — Bewertungen anderer Standorte wären dadurch fälschlich Frankfurt zugeschrieben worden. Ursache: beim Einbau in PR #21 nicht bedacht, dass jeder der 4 TOPWASH-Standorte ein eigenes, unabhängiges Google-Unternehmensprofil (und damit einen eigenen Bewertungs-Link) hat — nur ein einzelner vom Nutzer gelieferter Link wurde unreflektiert für den gesamten, standortübergreifenden Bewertungs-Bereich übernommen. Vor der Korrektur ausdrücklich nachgefragt statt zu raten, ob der bisherige Link wirklich Frankfurt zuzuordnen ist (bestätigt) und die 3 fehlenden Standort-Links angefordert; der Nutzer lieferte Eschborn und Neu-Isenburg direkt mit, Bad Nauheim auf Nachfrage nach. Umgesetzt: die zwei globalen Buttons ersetzt durch 4 einzelne Standort-Buttons (Bad Nauheim/Eschborn/Neu-Isenburg/Frankfurt), jeder mit dem korrekten, standorteigenen Google-Maps-Link. Lehre: bei einem Multi-Standort-Unternehmen niemals annehmen, dass eine standortbezogene externe Kennung (Bewertungslink, Place-ID, Social-Media-Profil o. Ä.) automatisch für alle Standorte gilt, nur weil nur ein Wert geliefert wurde — aktiv nachfragen, ob es sich um einen zentralen oder einen einzelnen Standort-Wert handelt, bevor er unternehmensweit ausgerollt wird.
